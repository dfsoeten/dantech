name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: Install Packwiz
        run: go install github.com/packwiz/packwiz@latest

      - name: Export Modpacks
        run: |
          TAG_NAME=${{ github.ref_name }}
          packwiz curseforge export --output "curseforge-${TAG_NAME}.zip"
          packwiz modrinth export --output "modrinth-${TAG_NAME}.mrpack"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true 
          files: |
            curseforge-${{ github.ref_name }}.zip
            modrinth-${{ github.ref_name }}.mrpack
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}